# {{ project_name }}

Generated by Fastango using the MVC template.

## Development

```bash
uv sync
cp .env.example .env
uv run fastapi dev app/main.py
```

## Quality

```bash
uv run pytest
uv run ruff check .
```

## API

- Health check: `GET /health`
- OpenAPI docs: `/docs`
- ReDoc: `/redoc`

## Architecture

Routes live in `app/api/routes`, schemas in `app/schemas`, business logic in `app/services`, and persistence code in `app/repositories`.

{% for section in readme_sections %}
{{ section }}

{% endfor %}
